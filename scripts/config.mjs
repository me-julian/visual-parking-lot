'use strict'

let config = {
    LOOP_SPEED: 50,
    carSpawnRate: {
        min: 5,
        max: 20,
    },
    handicapChance: 15,
    spawnCarCooldownTime: 2000,
    carParkingDurationRange: {
        min: 30,
        max: 180,
    },
}

export {config}
